# Spear autoelevate — no polkit, no interactive root shell
# Binary: /usr/local/bin/spear must be setuid root (mode 4755)
# Allowlist only: ffat-* field1-* elevate-status version
model=setuid-allowlist
polkit=off
sudo_prompt=off
